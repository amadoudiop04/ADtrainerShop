[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist/adtrainer-shop/browser"

# Redirige toutes les routes vers index.html pour le routing Angular (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
